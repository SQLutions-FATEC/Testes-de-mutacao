[project]
name = "Testes-de-mutacao"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mutmut>=3.6.0",
]

[tool.pytest.ini_options]
addopts = "--ignore=mutants"
